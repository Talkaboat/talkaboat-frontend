import { BigEqualizerPipe } from './big-equalizer.pipe';

describe('BigEqualizerPipe', () => {
  it('create an instance', () => {
    const pipe = new BigEqualizerPipe();
    expect(pipe).toBeTruthy();
  });
});
